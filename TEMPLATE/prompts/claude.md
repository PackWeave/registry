# System Prompt

Add your pack-specific system prompt here. This file is appended to the system prompt
for Claude Code and Gemini CLI. Use `prompts/system.md` instead if your prompt should
apply as a fallback across all supported CLIs.

Delete this file if your pack does not include a CLI-specific system prompt.
